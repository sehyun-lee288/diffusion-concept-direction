[tool.pytest.ini_options]
markers = [
    "gpu: tests that require a CUDA GPU and/or downloading pretrained weights",
]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
ignore = ["E501"]
